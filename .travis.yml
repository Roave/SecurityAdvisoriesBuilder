language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache/files

php:
  - 7.3

before_script:
  - composer install
  - git config --global user.email "ocramius+travis-ci@gmail.com"
  - git config --global user.name "Marco Pivetta (via Travis-CI)"

script:
  - vendor/bin/phpstan analyse
  - vendor/bin/phpunit
  - php build-conflicts.php
  - vendor/bin/phpcs
  - vendor/bin/infection --min-msi=99 --min-covered-msi=100
  - vendor/bin/roave-no-leaks
  - composer global require maglnet/composer-require-checker:^2.1.0 && composer-require-checker check composer.json
